diff --git disqus.js disqus.js
index 74d971d..deab80d 100644
--- disqus.js
+++ disqus.js
@@ -46,6 +46,9 @@ Drupal.behaviors.disqus = {
           if (settings.disqus.sso || false) {
             this.sso = settings.disqus.sso;
           }
+          if (typeof disqus_track_comments == 'function') {
+              this.callbacks.onNewComment = [function(params) { disqus_track_comments(params); }];
+          }
         };
 
         // Make the AJAX call to get the Disqus comments.
diff --git disqus.module disqus.module
index 93fb84a..7e9af5c 100644
--- disqus.module
+++ disqus.module
@@ -192,13 +192,18 @@ function disqus_node_load($nodes, $types) {
   if (!empty($domain)) {
     // Load Disqus into the nodes.
     foreach ($nodes as &$node) {
-      if (!empty($disqustypes[$node->type])) {
+      if (!empty($disqustypes[$node->type]) || (module_exists('comment_sources') && comment_sources_has_comments($node,'disqus'))) {
         // Save the data to the node object.
         $node->disqus = array('domain' => $domain);
 
         // Apply the Disqus status to the node.
-        $status = db_query("SELECT status FROM {disqus} WHERE nid = :nid", array(':nid' => $node->nid))->fetchObject();
-        $node->disqus['status'] = isset($status->status) ? (bool)$status->status : TRUE;
+        if (module_exists('comment_sources')) {
+          $node->disqus['status'] = comment_sources_should_display_comments($node,'disqus');
+        }
+        else {
+          $status = db_query("SELECT status FROM {disqus} WHERE nid = :nid", array(':nid' => $node->nid))->fetchObject();
+          $node->disqus['status'] = isset($status->status) ? (bool)$status->status : TRUE;
+        }
 
         // Build the absolute URL without the alias for the disqus_url flag.
         $node->disqus['url'] = url("node/$node->nid", array(
@@ -227,7 +232,8 @@ function disqus_node_load($nodes, $types) {
  * Implements hook_node_view().
  */
 function disqus_node_view($node, $view_mode) {
-  if (isset($node->disqus) && user_access('view disqus comments') && $node->disqus['status'] == 1) {
+  if (isset($node->disqus) && user_access('view disqus comments') && $node->disqus['status'] == 1
+      && (($node->comment_count == 0) || $node->comment = COMMENT_NODE_HIDDEN)) {
     switch ($view_mode) {
       case 'full':
       	// Inject Disqus into the node object.
@@ -295,6 +301,13 @@ function disqus_node_insert($node) {
     );
     drupal_write_record('disqus', $data);
   }
+
+  // Update node status for comment_sources module. Updated regardless of of whether set or not
+  if (module_exists('comment_sources')) {
+    if (isset($node->disqus_status)) {
+      comments_sources_set_node_status($node,'disqus',($node->disqus_status != FALSE));
+    }
+  }
 }
 
 /**
